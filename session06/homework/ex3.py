from turtle import*
def draw_square(length, Color):
    color(Color)
    for i in range(4):
        forward(length)
        right(90)


# length = 100
# Color = "red"
# draw_square(length, Color)
# mainloop()
